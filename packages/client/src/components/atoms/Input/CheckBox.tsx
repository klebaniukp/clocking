import React from 'react';

export const CheckBox = () => {
    return (
        <input
            onClick={() => {}}
            type='checkbox'
            className='form-check-input'
            id='form-checkbox'
        />
    );
};
